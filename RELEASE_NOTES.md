# Git Worktree 1.0.4

## 本次更新

- 修复插件面板中“打开分支”会再次弹出选择列表的问题。
- 右键入口改为直接打开当前选中的 Worktree，并同步调整文案。
- 打开 Java Maven Worktree 后，自动导入根目录 `pom.xml`，避免手动添加 Maven 项目。
- 同步插件名称与描述配置。

## 打包产物

- `build/distributions/git-worktree-intellij-1.0.4.zip`
